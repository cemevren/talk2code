apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "talk2code-backend.fullname" . }}
  labels:
    {{- include "talk2code-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "talk2code-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "talk2code-backend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
